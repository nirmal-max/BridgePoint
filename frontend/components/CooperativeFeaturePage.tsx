"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { usePathname, useRouter } from "next/navigation";
import { getActiveWorkspaceRole, useAuth } from "@/lib/auth-context";
import { api } from "@/lib/api";
import type { AdminPendingPayment, CertificationReview } from "@/lib/types";
import ProviderVerificationPanel from "@/components/ProviderVerificationPanel";

type Section = "members" | "worker-profile" | "verification" | "demand-forecast" | "workforce" | "revenue" | "analytics" | "jobs" | "training" | "settings" | "notifications" | "wage-benchmark";
const links = [["/admin", "Dashboard"], ["/admin/federation", "Federation"], ["/admin/societies", "Societies"], ["/admin/members", "Members / Workers"], ["/admin/jobs", "Job Management"], ["/admin/worker-profile", "Worker Profile"], ["/admin/verification", "Verification Center"], ["/admin/demand-forecast", "AI Demand Forecast"], ["/admin/workforce", "Workforce Allocation"], ["/admin/revenue", "Earnings & Revenue"], ["/admin/wage-benchmark", "Wage Benchmark"], ["/admin/training", "Training & Welfare"], ["/admin/analytics", "Analytics & Reports"], ["/admin/messages", "Messages"], ["/admin/notifications", "Notifications"], ["/admin/settings", "Settings"]] as const;
const copy: Record<Section, [string, string, string[]]> = {
  members: ["Members / Workers", "Manage cooperative members and verification progress.", ["Member directory", "Verification status", "Worker skills"]],
  "worker-profile": ["Worker Profile", "Review worker identity, skills, and cooperative membership.", ["Identity status", "Skill certifications", "Insurance status"]],
  verification: ["Verification Center", "Review worker documents and verification states.", ["Verified", "Pending review", "Needs attention"]],
  "demand-forecast": ["AI Demand Forecast", "Plan supply against expected service demand.", ["Predicted demand", "Forecast confidence", "Historical basis"]],
  workforce: ["Workforce Allocation", "Allocate verified workers using demand, skills, and availability.", ["Expected demand", "Available qualified workers", "Workforce gap"]],
  revenue: ["Earnings & Revenue", "Review cooperative revenue and recorded payment activity.", ["Cooperative share", "Platform commission", "Welfare and training fund"]],
  analytics: ["Analytics & Reports", "Track operational performance across your cooperative.", ["Jobs completed", "Worker growth", "Service performance"]],
  jobs: ["Job Management", "Review jobs and operational status from the existing job system.", ["Posted jobs", "Assigned jobs", "Completed jobs"]],
  training: ["Training & Welfare", "Track worker development and clearly separate live data from future provider integrations.", ["Skills", "Training pathways", "Welfare integrations are not connected"]],
  settings: ["Settings", "Review cooperative account and access settings.", ["Account access", "Notifications", "Backend permissions"]],
  notifications: ["Notifications", "Review persisted cooperative operations notifications.", ["Unread events", "Job updates", "Payment updates"]],
  "wage-benchmark": ["Wage Benchmark", "Compare recorded BridgePoint job values without inventing market statistics.", ["Average", "Median", "Recorded range"]],
};

export default function CooperativeFeaturePage({ section }: { section: Section }) {
  const router = useRouter();
  const { user, logout, loading } = useAuth();
  const pathname = usePathname();
  const [mobileNav, setMobileNav] = useState(false);
  const [data, setData] = useState<unknown>(null);
  const [error, setError] = useState("");
  const cooperativeAccess = !!user && (user.is_admin || user.roles?.includes("cooperative") || getActiveWorkspaceRole() === "cooperative");
  useEffect(() => { if (!loading && !user) router.replace(`/signin?role=cooperative&next=${encodeURIComponent(`/admin/${section}`)}`); else if (!loading && user && !cooperativeAccess) router.replace("/dashboard"); }, [cooperativeAccess, loading, router, section, user]);
  useEffect(() => { if (!user) return; const load = section === "members" ? api.getCooperativeMembers() : section === "demand-forecast" ? api.getDemandForecast() : section === "workforce" ? api.getWorkforceAllocation() : section === "analytics" ? api.getCooperativeAnalytics() : section === "notifications" ? api.getNotifications() : section === "wage-benchmark" ? api.getWageBenchmark(user.city || undefined) : section === "jobs" ? api.getAdminPending() : api.getCooperativeOverview(); load.then(setData).catch((err: unknown) => setError(err instanceof Error ? err.message : "Unable to load cooperative data.")); }, [section, user]);
  if (loading || !cooperativeAccess) return <div className="grid min-h-screen place-items-center bg-[#f3f7fd] text-slate-500">Checking cooperative access...</div>;
  const [title, description, cards] = copy[section];
  const rows = Array.isArray(data) ? data : data && typeof data === "object" ? Object.entries(data as Record<string, unknown>).map(([label, value]) => ({ label, value })) : [];
  const formatValue = (value: unknown): string => { if (value === null || value === undefined) return "Not available"; if (Array.isArray(value)) return value.map((item) => formatValue(item)).join(", "); if (typeof value === "object") return Object.entries(value as Record<string, unknown>).map(([key, item]) => `${key.replaceAll("_", " ")}: ${formatValue(item)}`).join(" · "); return String(value); };
  return <div className="min-h-screen bg-[#f3f7fd] text-slate-900"><div className="flex"><aside className={`${mobileNav ? "translate-x-0" : "-translate-x-full"} fixed inset-y-0 left-0 z-40 w-72 border-r bg-white p-5 transition-transform lg:static lg:block lg:translate-x-0`}><div className="flex items-center justify-between"><Link href="/admin" className="text-2xl font-semibold">Bridge<span className="text-blue-600">Point</span></Link><button aria-label="Close navigation" className="lg:hidden" onClick={() => setMobileNav(false)}>✕</button></div><p className="mb-7 text-sm text-slate-500">Work that matters. People who care.</p><nav className="space-y-1">{links.map(([href, label]) => <Link key={href} href={href} onClick={() => setMobileNav(false)} className={`block rounded-xl px-4 py-3 text-sm ${pathname === href ? "bg-blue-50 font-medium text-blue-700" : "text-slate-600 hover:bg-slate-50"}`}>{label}</Link>)}</nav><button onClick={logout} className="mt-8 w-full rounded-xl border border-red-200 px-3 py-2 text-left text-sm text-red-600">Sign Out</button></aside>{mobileNav && <button aria-label="Close navigation overlay" className="fixed inset-0 z-30 bg-slate-900/20 lg:hidden" onClick={() => setMobileNav(false)} />}<main className="min-w-0 flex-1"><header className="sticky top-0 z-20 border-b bg-white/90 px-5 py-4 backdrop-blur"><div className="flex items-center justify-between"><button aria-label="Open navigation" className="mr-3 rounded-lg border px-3 py-2 lg:hidden" onClick={() => setMobileNav(true)}>☰</button><Link href="/admin" className="text-xl font-semibold">Bridge<span className="text-blue-600">Point</span></Link><span className="ml-auto hidden text-sm text-slate-500 md:block">{user?.city || "Cooperative workspace"} · Admin</span></div></header><div className="mx-auto max-w-6xl space-y-5 p-5 md:p-8"><div className="rounded-3xl border bg-gradient-to-br from-white to-blue-50 p-6"><p className="text-sm text-blue-600">BridgePoint / Cooperative</p><h1 className="mt-2 text-3xl font-bold">{title}</h1><p className="mt-2 max-w-2xl text-slate-600">{description}</p></div>{error && <div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}{section === "jobs" && <PaymentOperationsPanel data={data as { jobs: AdminPendingPayment[]; total: number } | null} />}{section !== "jobs" && <section className="grid gap-4 md:grid-cols-3">{cards.map((card) => <article key={card} className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-semibold">{card}</h2><p className="mt-3 text-sm text-slate-500">Live data is loaded from the cooperative reporting API.</p></article>)}</section>}{section === "verification" && <><VerificationPanel /><ProviderVerificationPanel /></>}{section !== "jobs" && <div className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-semibold">Live cooperative data</h2>{data ? <div className="mt-4 grid gap-3 md:grid-cols-2">{rows.map((row, index) => <article key={index} className="rounded-xl border bg-slate-50 p-4"><div className="text-sm font-semibold capitalize text-slate-700">{String((row as { label?: unknown }).label || (row as Record<string, unknown>).name || "Record")}</div><div className="mt-2 text-sm text-slate-600">{formatValue((row as { value?: unknown }).value ?? row)}</div></article>)}</div> : <p className="mt-2 text-sm text-slate-600">Loading backend data...</p>}</div>}</div></main></div></div>;
}

function PaymentOperationsPanel({ data }: { data: { jobs: AdminPendingPayment[]; total: number } | null }) {
  const [items, setItems] = useState<AdminPendingPayment[]>([]);
  const [action, setAction] = useState<number | null>(null);
  const [error, setError] = useState("");
  useEffect(() => { if (data) setItems(data.jobs); }, [data]);
  async function refresh() { try { const result = await api.getAdminPending(); setItems(result.jobs); } catch (err: unknown) { setError(err instanceof Error ? err.message : "Unable to refresh payment operations."); } }
  async function verify(id: number) { setAction(id); setError(""); try { await api.adminVerifyPayment(id); await refresh(); } catch (err: unknown) { setError(err instanceof Error ? err.message : "Unable to verify payment."); } finally { setAction(null); } }
  async function release(id: number) { setAction(id); setError(""); try { await api.adminReleasePayout(id); await refresh(); } catch (err: unknown) { setError(err instanceof Error ? err.message : "Unable to release payout."); } finally { setAction(null); } }
  const statusLabel = (status: string) => ({ payment_in_process: "Pending payment", verification_pending: "Payment received / awaiting verification", verified: "Payment verified / payout pending", payout_released: "Worker payout completed" }[status] || status);
  return <section className="space-y-4"><div className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-semibold">Payment Operations</h2><p className="mt-2 text-sm text-slate-600">Customer payment is received by BridgePoint admin, then manually verified before the worker payout is released.</p><p className="mt-2 text-xs text-slate-500">Payment is only released after work is completed. If work is not completed, do not release the payout; any return is an admin-managed return to the original payer.</p>{error && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}</div>{data === null ? <div className="rounded-2xl border bg-white p-6 text-slate-500">Loading payment operations...</div> : items.length === 0 ? <div className="rounded-2xl border bg-white p-6 text-slate-500">No pending payment operations.</div> : items.map((item) => <article key={item.id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="text-lg font-semibold">{item.title}</h3><p className="text-sm text-slate-500">Customer: {item.employer_name || "Not available"} · Worker: {item.worker_name || "Not assigned"}</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">{statusLabel(item.status)}</span></div><div className="mt-4 grid gap-3 text-sm sm:grid-cols-2 lg:grid-cols-4"><div><p className="text-slate-500">Invoice amount</p><b>₹{item.employer_total.toLocaleString("en-IN")}</b></div><div><p className="text-slate-500">Worker payout</p><b>₹{item.worker_payout.toLocaleString("en-IN")}</b></div><div><p className="text-slate-500">Registered payment number</p><b>{item.worker_payment_number || "Not registered"}</b></div><div><p className="text-slate-500">Transaction reference</p><b>{item.upi_reference || "Not submitted"}</b></div></div><div className="mt-4 flex flex-wrap gap-2">{item.status === "verification_pending" && <button disabled={action === item.id} onClick={() => verify(item.id)} className="rounded-xl bg-blue-600 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{action === item.id ? "Verifying..." : "Confirm payment received"}</button>}{item.status === "verified" && <button disabled={action === item.id} onClick={() => release(item.id)} className="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{action === item.id ? "Releasing..." : "Release worker payout"}</button>}<Link href={`/jobs/${item.id}`} className="rounded-xl border border-blue-300 px-4 py-2 text-sm font-semibold text-blue-700">View job</Link></div></article>)}</section>;
}

function VerificationPanel() { const [items, setItems] = useState<CertificationReview[]>([]); const [error, setError] = useState(""); const [saving, setSaving] = useState<number | null>(null); useEffect(() => { api.getCertificationQueue().then(setItems).catch((err: unknown) => setError(err instanceof Error ? err.message : "Unable to load certification queue.")); }, []); async function verify(id: number) { setSaving(id); setError(""); try { const updated = await api.verifyCertification(id); setItems((current) => current.map((item) => item.id === id ? { ...item, ...updated, worker_name: item.worker_name } : item)); } catch (err) { setError(err instanceof Error ? err.message : "Unable to verify certification."); } finally { setSaving(null); } } return <section className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-semibold">Certification review queue</h2>{error && <p className="mt-3 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}{items.length === 0 ? <p className="mt-4 text-sm text-slate-500">No worker certifications are waiting for review.</p> : <div className="mt-4 space-y-3">{items.map((item) => <article key={item.id} className="flex flex-wrap items-center justify-between gap-4 rounded-xl bg-slate-50 p-4"><div><b>{item.name}</b><p className="text-sm text-slate-600">{item.worker_name} · {item.issuing_organization}</p><p className="text-xs text-slate-500">{item.verification_status} · issued {item.issue_date}</p></div>{item.verification_status !== "VERIFIED" && <button disabled={saving === item.id} onClick={() => verify(item.id)} className="rounded-lg bg-blue-600 px-3 py-2 text-sm font-semibold text-white disabled:opacity-50">{saving === item.id ? "Verifying..." : "Verify"}</button>}</article>)}</div>}</section>; }
