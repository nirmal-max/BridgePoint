"use client";

import { useEffect, useState } from "react";
import { useAuth } from "@/lib/auth-context";
import { api } from "@/lib/api";
import type { EmergencyRequest } from "@/lib/types";

export default function EmergencyPage() {
  const { user, loading } = useAuth();
  const [items, setItems] = useState<EmergencyRequest[]>([]);
  const [error, setError] = useState("");
  const [saved, setSaved] = useState("");
  const [form, setForm] = useState({ category: "Electrical emergency", urgency: "high", description: "", address: "", city: "" });
  useEffect(() => { if (user) api.getMyEmergencies().then(setItems).catch((err: unknown) => setError(err instanceof Error ? err.message : "Unable to load emergency requests.")); }, [user]);
  async function submit(event: React.FormEvent) { event.preventDefault(); setError(""); setSaved(""); try { const item = await api.createEmergency({ ...form, city: form.city || user?.city || "Chennai" }); setItems((current) => [item, ...current]); setForm({ ...form, description: "", address: "" }); setSaved("Emergency request submitted. Matching workers in your city can respond."); } catch (err) { setError(err instanceof Error ? err.message : "Unable to create emergency request."); } }
  if (loading || !user) return <div className="grid min-h-screen place-items-center bg-[#f3f8ff] text-slate-500">Checking access...</div>;
  return <main className="min-h-screen bg-[#f3f8ff] px-5 py-8 text-slate-900"><div className="mx-auto max-w-4xl"><header className="mb-6 flex items-center justify-between"><a href="/dashboard" className="text-2xl font-bold">Bridge<span className="text-blue-600">Point</span></a><a href="/dashboard" className="text-sm text-blue-600">Back to dashboard</a></header><section className="rounded-3xl border bg-white p-6 shadow-sm"><p className="text-sm font-medium text-red-600">Emergency service</p><h1 className="mt-1 text-3xl font-bold">Request verified nearby help</h1><p className="mt-2 text-slate-600">Enter a manual address. BridgePoint does not claim live GPS dispatch.</p>{error && <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}{saved && <p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{saved}</p>}<form onSubmit={submit} className="mt-6 grid gap-4 md:grid-cols-2"><label className="text-sm font-medium">Category<select value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })} className="mt-2 w-full rounded-xl border px-3 py-3"><option>Electrical emergency</option><option>Plumbing emergency</option><option>Medical support</option><option>Other urgent help</option></select></label><label className="text-sm font-medium">Urgency<select value={form.urgency} onChange={(e) => setForm({ ...form, urgency: e.target.value })} className="mt-2 w-full rounded-xl border px-3 py-3"><option value="critical">Critical</option><option value="high">High</option><option value="normal">Normal</option></select></label><label className="text-sm font-medium md:col-span-2">City<input required value={form.city} onChange={(e) => setForm({ ...form, city: e.target.value })} className="mt-2 w-full rounded-xl border px-3 py-3" /></label><label className="text-sm font-medium md:col-span-2">Address<input required value={form.address} onChange={(e) => setForm({ ...form, address: e.target.value })} placeholder="Enter the location for help" className="mt-2 w-full rounded-xl border px-3 py-3" /></label><label className="text-sm font-medium md:col-span-2">What happened?<textarea required minLength={10} value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} className="mt-2 min-h-28 w-full rounded-xl border px-3 py-3" /></label><button className="rounded-xl bg-red-600 px-5 py-3 font-semibold text-white md:col-span-2">Submit emergency request</button></form></section><section className="mt-6 space-y-3"><h2 className="text-xl font-bold">Your requests</h2>{items.length === 0 ? <p className="rounded-2xl border bg-white p-5 text-sm text-slate-500">No emergency requests yet.</p> : items.map((item) => <article key={item.id} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex justify-between gap-3"><b>{item.category}</b><span className="text-sm text-slate-500">{item.status}</span></div><p className="mt-2 text-sm text-slate-600">{item.description}</p><p className="mt-2 text-sm text-slate-500">{item.address} · {item.city}</p></article>)}</section></div></main>;
}
